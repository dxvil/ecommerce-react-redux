import React from 'react'

const UndoBtn = ({ method }) => {
    return (
        <button className='circular ui icon button shop-undo-btn'>
            <i className='icon undo'></i>
        </button>
    )
}
export default UndoBtn
